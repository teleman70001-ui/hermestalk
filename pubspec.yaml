name: hermestalk
description: HermesTalk — speech-to-speech AI assistant chatting back-to-back with Hermes Agent
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  http: ^1.2.3
  speech_to_text: ^7.1.0
  flutter_tts: ^4.2.5
  shared_preferences: ^2.5.3
  permission_handler: ^11.3.1
  flutter_secure_storage: ^9.2.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
